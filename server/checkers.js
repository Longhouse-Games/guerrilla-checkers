var CheckersServer = function(GameState, id, protocol) {
  var me = this;
  me.state = new GameState();
  me.socket = socket;
};

CheckersServer.prototype.VERSION = "1.0.0";

CheckersServer.prototype.start() {
};
